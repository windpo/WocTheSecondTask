server:
  port: 5050
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #注意在配置时，login为数组库名称，数据库id应和此处配置匹配
    url: jdbc:mysql://localhost:3306/login?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: hanser
    #访问静态页面
  web:
    resources:
      static-locations: classpath:/templates